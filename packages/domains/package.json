{
  "name": "@ws/domains",
  "version": "0.0.0",
  "main": "dist/index",
  "types": "dist",
  "scripts": {
    "build": "bun clean && bun compile",
    "build:watch": "bun compile -- -w",
    "clean": "rm -rf ./dist && rm -rf tsconfig.build.tsbuildinfo",
    "compile": "tsc -p ./tsconfig.build.json"
  },
  "devDependencies": {
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "@nestjs/typeorm": "^10.0.1",
    "@tsconfig/node20": "20.1.4",
    "nanoid": "5.0.7",
    "typeorm": "^0.3.17"
  }
}
